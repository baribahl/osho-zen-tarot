// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://osho.barib.art',
  // GitHub Pages deployment config (temporarily use repo path if custom domain not set)
  // base: '/osho-zen-tarot/',
});
